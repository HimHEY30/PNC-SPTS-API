$kind: http-request
description: "Generated from cURL: curl -X GET {{base_url}}/users/profile"
url: "{{base_url}}/api/users/me"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 401", function () {
          pm.response.to.have.status(401);
      });
    language: text/javascript
order: 13000
