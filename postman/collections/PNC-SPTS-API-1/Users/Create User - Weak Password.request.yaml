$kind: http-request
description: "Generated from cURL: curl -X POST {{base_url}}/users -H \"Content-Type: application/json\" -H \"Authorization: Bearer {{access_token}}\" -d '{\"first_name\": \"Test\", \"last_name\": \"User\", \"email\": \"weakpass@example.com\", \"password\": \"weak\", \"role\": \"TUTOR\"}'"
url: "{{base_url}}/api/users/00000000-0000-0000-0000-000000000000"
method: GET
headers:
  Content-Type: application/json
  Authorization: Bearer {{access_token}}
body:
  type: json
  content: '{ "first_name": "Test", "last_name": "User", "email": "weakpass@example.com", "password": "weak", "role": "TUTOR" }'
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 400", function () {
          pm.response.to.have.status(400);
      });
      pm.test("Password validation error", function () {
          const json = pm.response.json();
          pm.expect(json.success).to.be.false;
      });
    language: text/javascript
order: 7000
