$kind: http-request
description: 'Generated from cURL: curl -X GET {{base_url}}/users -H "Authorization: Bearer {{access_token}}"'
url: "{{base_url}}/api/users"
method: GET
headers:
  Authorization: Bearer {{access_token}}
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });
      pm.test("Response is successful", function () {
          const json = pm.response.json();
          pm.expect(json.success).to.be.true;
      });
      pm.test("Data is an array", function () {
          const json = pm.response.json();
          pm.expect(json.data).to.be.an('array');
      });
      // Save first user ID for later tests
      const json = pm.response.json();
      if (json.success && json.data && json.data.length > 0) {
          pm.collectionVariables.set('test_user_id', json.data[0].user_id || json.data[0].id);
          console.log('✅ Saved test_user_id: ' + pm.collectionVariables.get('test_user_id'));
      }
    language: text/javascript
order: 12000
